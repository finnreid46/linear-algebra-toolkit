[project]
name = "linear-algebra-toolkit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "finnreid46", email = "finnreid46@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
linear-algebra-toolkit = "linear_algebra_toolkit:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
